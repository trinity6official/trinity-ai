services:
  - type: web
    name: trinity-voice-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn core.api:app --workers 1 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT
    healthCheckPath: /api/status
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false          # set manually in Render dashboard
      - key: GOOGLE_API_KEY
        sync: false          # set manually in Render dashboard
      - key: TRINITY_JWT_SECRET
        generateValue: true  # Render auto-generates a random secret
      - key: TRINITY_APP_PIN_HASH
        sync: false          # set manually: echo -n "yourpin" | sha256sum
      - key: PYTHON_VERSION
        value: "3.11.9"
